modLoader="javafml" #mandatory

loaderVersion="[35,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.

license="mit"

issueTrackerURL="https://github.com/SrGnis/Creeper_AI_Updated/issues" #optional

[[mods]] #mandatory

modId="creeperaiupdated" #mandatory

version="1.3" #mandatory

displayName="Creeper AI Updated" #mandatory

updateJSONURL="http://myurl.me/" #optional

displayURL="https://www.curseforge.com/minecraft/mc-mods/creeper-ai-updated" #optional

logoFile="logo.png" #optional

credits="Mod inspired by the work of Funwayguy in his mod Epic Siege Mod" #optional

authors="SrGnis" #optional

description='''Gives creepers the ability of see trough walls, spawn powered and explode if they can't reach you, destroying obstacles and walls.'''

# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.creeperaiupdated]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[35,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.creeperaiupdated]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.16.4,1.17)"
    ordering="NONE"
    side="BOTH"
